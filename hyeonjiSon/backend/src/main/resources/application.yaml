server:
  port: 7777

spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/recycle_proj?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    url: jdbc:mysql://localhost:3306/recycle_proj?allowpublickeyretrieval=true&usessl=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    #username: eddi
    username: root
    #password: eddi@123
    password: khWeb@123
    driver-class-name: com.mysql.cj.jdbc.Driver
    dialect: org.hibernate.dialect.MySQL57Dialect

    jpa:
      open-in-view: true
      database: mysql
      show-sql: true
      generate-ddl: true
      hibernate:
        #ddl-auto: update
        ddl-auto: create

    servlet:
      multipart:
        max-file-size: 20MB
        max-request-size: 20MB